# Core dependencies only — everything else (pyrogram, telethon, aiogram, python-telegram-bot,
# and 50+ other common packages) is auto-detected and installed on demand per-script, not
# bundled here. Keeping this minimal matters for low-spec hosting: a platform that builds
# from this file directly (Nixpacks-style, when no Dockerfile is used) shouldn't end up
# pulling in a much heavier dependency set than the Dockerfile actually installs.
pyTelegramBotAPI>=4.14.0
python-dotenv>=1.0.0
psutil>=5.9.0
requests>=2.31.0
flask>=3.0.0
